services:
  - type: web
    name: applepay-demo
    env: nixpacks
    buildCommand: "mvn -ntp -DskipTests package"
    startCommand: "java -jar target/*.jar"
    envVars:
      - key: PORT
        value: 8080
